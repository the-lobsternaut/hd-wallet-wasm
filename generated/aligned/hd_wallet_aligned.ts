/**
 * Auto-generated aligned buffer accessors for HD Wallet WASM
 *
 * DO NOT EDIT - Generated by scripts/generate-aligned.mjs
 * Source: schemas/hd_wallet_aligned.fbs
 */

/**
 * Auto-generated aligned buffer accessors
 * Use with WebAssembly.Memory for zero-copy access
 */

export const Curve = {
  SECP256K1: 0,
  ED25519: 1,
  P256: 2,
  P384: 3,
  X25519: 4,
} as const;
export type Curve = typeof Curve[keyof typeof Curve];

export const Error = {
  OK: 0,
  UNKNOWN: 1,
  INVALID_ARGUMENT: 2,
  NOT_SUPPORTED: 3,
  OUT_OF_MEMORY: 4,
  INTERNAL: 5,
  NO_ENTROPY: 100,
  INSUFFICIENT_ENTROPY: 101,
  INVALID_WORD: 200,
  INVALID_CHECKSUM: 201,
  INVALID_MNEMONIC_LENGTH: 202,
  INVALID_ENTROPY_LENGTH: 203,
  INVALID_SEED: 300,
  INVALID_PATH: 301,
  INVALID_CHILD_INDEX: 302,
  HARDENED_FROM_PUBLIC: 303,
  INVALID_EXTENDED_KEY: 304,
  INVALID_PRIVATE_KEY: 400,
  INVALID_PUBLIC_KEY: 401,
  INVALID_SIGNATURE: 402,
  VERIFICATION_FAILED: 403,
  KEY_DERIVATION_FAILED: 404,
} as const;
export type Error = typeof Error[keyof typeof Error];

export const AddressType = {
  P2PKH: 0,
  P2SH: 1,
  P2WPKH: 2,
  P2WSH: 3,
  P2TR: 4,
} as const;
export type AddressType = typeof AddressType[keyof typeof AddressType];

export const Network = {
  MAINNET: 0,
  TESTNET: 1,
} as const;
export type Network = typeof Network[keyof typeof Network];

export const BYTES32_SIZE = 32;
export const BYTES32_ALIGN = 1;

export const Bytes32Offsets = {
  data: 0,
} as const;

export class Bytes32View {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 32);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): Bytes32View {
    return new Bytes32View(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): Bytes32View {
    return new Bytes32View(bytes.buffer, bytes.byteOffset + offset);
  }

  get data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 0, 32);
  }

  toObject(): Record<string, unknown> {
    return {
      data: Array.from(this.data),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.data !== undefined) {
      const arr = this.data;
      const src = obj.data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): Bytes32View {
    return new Bytes32View(new ArrayBuffer(32));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 32);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 32);
  }
}

export class Bytes32ArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): Bytes32ArrayView {
    return new Bytes32ArrayView(memory.buffer, ptr, count);
  }

  at(index: number): Bytes32View {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new Bytes32View(this.buffer, this.baseOffset + index * 32);
  }

  *[Symbol.iterator](): Iterator<Bytes32View> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const BYTES33_SIZE = 33;
export const BYTES33_ALIGN = 1;

export const Bytes33Offsets = {
  data: 0,
} as const;

export class Bytes33View {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 33);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): Bytes33View {
    return new Bytes33View(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): Bytes33View {
    return new Bytes33View(bytes.buffer, bytes.byteOffset + offset);
  }

  get data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 0, 33);
  }

  toObject(): Record<string, unknown> {
    return {
      data: Array.from(this.data),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.data !== undefined) {
      const arr = this.data;
      const src = obj.data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): Bytes33View {
    return new Bytes33View(new ArrayBuffer(33));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 33);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 33);
  }
}

export class Bytes33ArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): Bytes33ArrayView {
    return new Bytes33ArrayView(memory.buffer, ptr, count);
  }

  at(index: number): Bytes33View {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new Bytes33View(this.buffer, this.baseOffset + index * 33);
  }

  *[Symbol.iterator](): Iterator<Bytes33View> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const BYTES64_SIZE = 64;
export const BYTES64_ALIGN = 1;

export const Bytes64Offsets = {
  data: 0,
} as const;

export class Bytes64View {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 64);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): Bytes64View {
    return new Bytes64View(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): Bytes64View {
    return new Bytes64View(bytes.buffer, bytes.byteOffset + offset);
  }

  get data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 0, 64);
  }

  toObject(): Record<string, unknown> {
    return {
      data: Array.from(this.data),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.data !== undefined) {
      const arr = this.data;
      const src = obj.data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): Bytes64View {
    return new Bytes64View(new ArrayBuffer(64));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 64);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 64);
  }
}

export class Bytes64ArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): Bytes64ArrayView {
    return new Bytes64ArrayView(memory.buffer, ptr, count);
  }

  at(index: number): Bytes64View {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new Bytes64View(this.buffer, this.baseOffset + index * 64);
  }

  *[Symbol.iterator](): Iterator<Bytes64View> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const EXTENDEDKEYDATA_SIZE = 116;
export const EXTENDEDKEYDATA_ALIGN = 4;

export const ExtendedKeyDataOffsets = {
  curve: 0,
  depth: 1,
  _pad0: 2,
  parent_fingerprint: 4,
  child_index: 8,
  chain_code_data: 12,
  public_key_data: 44,
  _pad1: 77,
  private_key_data: 80,
  has_private_key: 112,
  _pad2: 113,
} as const;

export class ExtendedKeyDataView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 116);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): ExtendedKeyDataView {
    return new ExtendedKeyDataView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): ExtendedKeyDataView {
    return new ExtendedKeyDataView(bytes.buffer, bytes.byteOffset + offset);
  }

  get curve(): number {
    return this.view.getUint8(0);
  }
  set curve(v: number) {
    this.view.setUint8(0, v);
  }

  get depth(): number {
    return this.view.getUint8(1);
  }
  set depth(v: number) {
    this.view.setUint8(1, v);
  }

  get _pad0(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 2, 2);
  }

  get parent_fingerprint(): number {
    return this.view.getUint32(4, true);
  }
  set parent_fingerprint(v: number) {
    this.view.setUint32(4, v, true);
  }

  get child_index(): number {
    return this.view.getUint32(8, true);
  }
  set child_index(v: number) {
    this.view.setUint32(8, v, true);
  }

  get chain_code_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 12, 32);
  }

  get public_key_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 44, 33);
  }

  get _pad1(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 77, 3);
  }

  get private_key_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 80, 32);
  }

  get has_private_key(): number {
    return this.view.getUint8(112);
  }
  set has_private_key(v: number) {
    this.view.setUint8(112, v);
  }

  get _pad2(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 113, 3);
  }

  toObject(): Record<string, unknown> {
    return {
      curve: this.curve,
      depth: this.depth,
      _pad0: Array.from(this._pad0),
      parent_fingerprint: this.parent_fingerprint,
      child_index: this.child_index,
      chain_code_data: Array.from(this.chain_code_data),
      public_key_data: Array.from(this.public_key_data),
      _pad1: Array.from(this._pad1),
      private_key_data: Array.from(this.private_key_data),
      has_private_key: this.has_private_key,
      _pad2: Array.from(this._pad2),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.curve !== undefined) this.curve = obj.curve as number;
    if (obj.depth !== undefined) this.depth = obj.depth as number;
    if (obj._pad0 !== undefined) {
      const arr = this._pad0;
      const src = obj._pad0 as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.parent_fingerprint !== undefined) this.parent_fingerprint = obj.parent_fingerprint as number;
    if (obj.child_index !== undefined) this.child_index = obj.child_index as number;
    if (obj.chain_code_data !== undefined) {
      const arr = this.chain_code_data;
      const src = obj.chain_code_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.public_key_data !== undefined) {
      const arr = this.public_key_data;
      const src = obj.public_key_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj._pad1 !== undefined) {
      const arr = this._pad1;
      const src = obj._pad1 as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.private_key_data !== undefined) {
      const arr = this.private_key_data;
      const src = obj.private_key_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.has_private_key !== undefined) this.has_private_key = obj.has_private_key as number;
    if (obj._pad2 !== undefined) {
      const arr = this._pad2;
      const src = obj._pad2 as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): ExtendedKeyDataView {
    return new ExtendedKeyDataView(new ArrayBuffer(116));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 116);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 116);
  }
}

export class ExtendedKeyDataArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): ExtendedKeyDataArrayView {
    return new ExtendedKeyDataArrayView(memory.buffer, ptr, count);
  }

  at(index: number): ExtendedKeyDataView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new ExtendedKeyDataView(this.buffer, this.baseOffset + index * 116);
  }

  *[Symbol.iterator](): Iterator<ExtendedKeyDataView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const BATCHDERIVEREQUEST_SIZE = 132;
export const BATCHDERIVEREQUEST_ALIGN = 4;

export const BatchDeriveRequestOffsets = {
  base_key_curve: 0,
  base_key_depth: 1,
  base_key__pad0: 2,
  base_key_parent_fingerprint: 4,
  base_key_child_index: 8,
  base_key_chain_code_data: 12,
  base_key_public_key_data: 44,
  base_key__pad1: 77,
  base_key_private_key_data: 80,
  base_key_has_private_key: 112,
  base_key__pad2: 113,
  start_index: 116,
  count: 120,
  hardened: 124,
  _pad: 125,
} as const;

export class BatchDeriveRequestView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 132);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): BatchDeriveRequestView {
    return new BatchDeriveRequestView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): BatchDeriveRequestView {
    return new BatchDeriveRequestView(bytes.buffer, bytes.byteOffset + offset);
  }

  get base_key_curve(): number {
    return this.view.getUint8(0);
  }
  set base_key_curve(v: number) {
    this.view.setUint8(0, v);
  }

  get base_key_depth(): number {
    return this.view.getUint8(1);
  }
  set base_key_depth(v: number) {
    this.view.setUint8(1, v);
  }

  get base_key__pad0(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 2, 2);
  }

  get base_key_parent_fingerprint(): number {
    return this.view.getUint32(4, true);
  }
  set base_key_parent_fingerprint(v: number) {
    this.view.setUint32(4, v, true);
  }

  get base_key_child_index(): number {
    return this.view.getUint32(8, true);
  }
  set base_key_child_index(v: number) {
    this.view.setUint32(8, v, true);
  }

  get base_key_chain_code_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 12, 32);
  }

  get base_key_public_key_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 44, 33);
  }

  get base_key__pad1(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 77, 3);
  }

  get base_key_private_key_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 80, 32);
  }

  get base_key_has_private_key(): number {
    return this.view.getUint8(112);
  }
  set base_key_has_private_key(v: number) {
    this.view.setUint8(112, v);
  }

  get base_key__pad2(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 113, 3);
  }

  get start_index(): number {
    return this.view.getUint32(116, true);
  }
  set start_index(v: number) {
    this.view.setUint32(116, v, true);
  }

  get count(): number {
    return this.view.getUint32(120, true);
  }
  set count(v: number) {
    this.view.setUint32(120, v, true);
  }

  get hardened(): number {
    return this.view.getUint8(124);
  }
  set hardened(v: number) {
    this.view.setUint8(124, v);
  }

  get _pad(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 125, 7);
  }

  toObject(): Record<string, unknown> {
    return {
      base_key_curve: this.base_key_curve,
      base_key_depth: this.base_key_depth,
      base_key__pad0: Array.from(this.base_key__pad0),
      base_key_parent_fingerprint: this.base_key_parent_fingerprint,
      base_key_child_index: this.base_key_child_index,
      base_key_chain_code_data: Array.from(this.base_key_chain_code_data),
      base_key_public_key_data: Array.from(this.base_key_public_key_data),
      base_key__pad1: Array.from(this.base_key__pad1),
      base_key_private_key_data: Array.from(this.base_key_private_key_data),
      base_key_has_private_key: this.base_key_has_private_key,
      base_key__pad2: Array.from(this.base_key__pad2),
      start_index: this.start_index,
      count: this.count,
      hardened: this.hardened,
      _pad: Array.from(this._pad),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.base_key_curve !== undefined) this.base_key_curve = obj.base_key_curve as number;
    if (obj.base_key_depth !== undefined) this.base_key_depth = obj.base_key_depth as number;
    if (obj.base_key__pad0 !== undefined) {
      const arr = this.base_key__pad0;
      const src = obj.base_key__pad0 as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.base_key_parent_fingerprint !== undefined) this.base_key_parent_fingerprint = obj.base_key_parent_fingerprint as number;
    if (obj.base_key_child_index !== undefined) this.base_key_child_index = obj.base_key_child_index as number;
    if (obj.base_key_chain_code_data !== undefined) {
      const arr = this.base_key_chain_code_data;
      const src = obj.base_key_chain_code_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.base_key_public_key_data !== undefined) {
      const arr = this.base_key_public_key_data;
      const src = obj.base_key_public_key_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.base_key__pad1 !== undefined) {
      const arr = this.base_key__pad1;
      const src = obj.base_key__pad1 as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.base_key_private_key_data !== undefined) {
      const arr = this.base_key_private_key_data;
      const src = obj.base_key_private_key_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.base_key_has_private_key !== undefined) this.base_key_has_private_key = obj.base_key_has_private_key as number;
    if (obj.base_key__pad2 !== undefined) {
      const arr = this.base_key__pad2;
      const src = obj.base_key__pad2 as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.start_index !== undefined) this.start_index = obj.start_index as number;
    if (obj.count !== undefined) this.count = obj.count as number;
    if (obj.hardened !== undefined) this.hardened = obj.hardened as number;
    if (obj._pad !== undefined) {
      const arr = this._pad;
      const src = obj._pad as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): BatchDeriveRequestView {
    return new BatchDeriveRequestView(new ArrayBuffer(132));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 132);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 132);
  }
}

export class BatchDeriveRequestArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): BatchDeriveRequestArrayView {
    return new BatchDeriveRequestArrayView(memory.buffer, ptr, count);
  }

  at(index: number): BatchDeriveRequestView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new BatchDeriveRequestView(this.buffer, this.baseOffset + index * 132);
  }

  *[Symbol.iterator](): Iterator<BatchDeriveRequestView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const DERIVEDKEYENTRY_SIZE = 76;
export const DERIVEDKEYENTRY_ALIGN = 4;

export const DerivedKeyEntryOffsets = {
  index: 0,
  error: 4,
  public_key_data: 8,
  _pad0: 41,
  private_key_data: 44,
} as const;

export class DerivedKeyEntryView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 76);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): DerivedKeyEntryView {
    return new DerivedKeyEntryView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): DerivedKeyEntryView {
    return new DerivedKeyEntryView(bytes.buffer, bytes.byteOffset + offset);
  }

  get index(): number {
    return this.view.getUint32(0, true);
  }
  set index(v: number) {
    this.view.setUint32(0, v, true);
  }

  get error(): number {
    return this.view.getInt32(4, true);
  }
  set error(v: number) {
    this.view.setInt32(4, v, true);
  }

  get public_key_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 8, 33);
  }

  get _pad0(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 41, 3);
  }

  get private_key_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 44, 32);
  }

  toObject(): Record<string, unknown> {
    return {
      index: this.index,
      error: this.error,
      public_key_data: Array.from(this.public_key_data),
      _pad0: Array.from(this._pad0),
      private_key_data: Array.from(this.private_key_data),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.index !== undefined) this.index = obj.index as number;
    if (obj.error !== undefined) this.error = obj.error as number;
    if (obj.public_key_data !== undefined) {
      const arr = this.public_key_data;
      const src = obj.public_key_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj._pad0 !== undefined) {
      const arr = this._pad0;
      const src = obj._pad0 as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.private_key_data !== undefined) {
      const arr = this.private_key_data;
      const src = obj.private_key_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): DerivedKeyEntryView {
    return new DerivedKeyEntryView(new ArrayBuffer(76));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 76);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 76);
  }
}

export class DerivedKeyEntryArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): DerivedKeyEntryArrayView {
    return new DerivedKeyEntryArrayView(memory.buffer, ptr, count);
  }

  at(index: number): DerivedKeyEntryView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new DerivedKeyEntryView(this.buffer, this.baseOffset + index * 76);
  }

  *[Symbol.iterator](): Iterator<DerivedKeyEntryView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const BATCHSIGNREQUEST_SIZE = 40;
export const BATCHSIGNREQUEST_ALIGN = 4;

export const BatchSignRequestOffsets = {
  private_key_data: 0,
  curve: 32,
  _pad: 33,
  count: 36,
} as const;

export class BatchSignRequestView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 40);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): BatchSignRequestView {
    return new BatchSignRequestView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): BatchSignRequestView {
    return new BatchSignRequestView(bytes.buffer, bytes.byteOffset + offset);
  }

  get private_key_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 0, 32);
  }

  get curve(): number {
    return this.view.getUint8(32);
  }
  set curve(v: number) {
    this.view.setUint8(32, v);
  }

  get _pad(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 33, 3);
  }

  get count(): number {
    return this.view.getUint32(36, true);
  }
  set count(v: number) {
    this.view.setUint32(36, v, true);
  }

  toObject(): Record<string, unknown> {
    return {
      private_key_data: Array.from(this.private_key_data),
      curve: this.curve,
      _pad: Array.from(this._pad),
      count: this.count,
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.private_key_data !== undefined) {
      const arr = this.private_key_data;
      const src = obj.private_key_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.curve !== undefined) this.curve = obj.curve as number;
    if (obj._pad !== undefined) {
      const arr = this._pad;
      const src = obj._pad as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.count !== undefined) this.count = obj.count as number;
  }

  static allocate(): BatchSignRequestView {
    return new BatchSignRequestView(new ArrayBuffer(40));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 40);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 40);
  }
}

export class BatchSignRequestArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): BatchSignRequestArrayView {
    return new BatchSignRequestArrayView(memory.buffer, ptr, count);
  }

  at(index: number): BatchSignRequestView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new BatchSignRequestView(this.buffer, this.baseOffset + index * 40);
  }

  *[Symbol.iterator](): Iterator<BatchSignRequestView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const HASHENTRY_SIZE = 36;
export const HASHENTRY_ALIGN = 4;

export const HashEntryOffsets = {
  index: 0,
  hash_data: 4,
} as const;

export class HashEntryView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 36);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): HashEntryView {
    return new HashEntryView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): HashEntryView {
    return new HashEntryView(bytes.buffer, bytes.byteOffset + offset);
  }

  get index(): number {
    return this.view.getUint32(0, true);
  }
  set index(v: number) {
    this.view.setUint32(0, v, true);
  }

  get hash_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 4, 32);
  }

  toObject(): Record<string, unknown> {
    return {
      index: this.index,
      hash_data: Array.from(this.hash_data),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.index !== undefined) this.index = obj.index as number;
    if (obj.hash_data !== undefined) {
      const arr = this.hash_data;
      const src = obj.hash_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): HashEntryView {
    return new HashEntryView(new ArrayBuffer(36));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 36);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 36);
  }
}

export class HashEntryArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): HashEntryArrayView {
    return new HashEntryArrayView(memory.buffer, ptr, count);
  }

  at(index: number): HashEntryView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new HashEntryView(this.buffer, this.baseOffset + index * 36);
  }

  *[Symbol.iterator](): Iterator<HashEntryView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const SIGNATUREENTRY_SIZE = 76;
export const SIGNATUREENTRY_ALIGN = 4;

export const SignatureEntryOffsets = {
  index: 0,
  error: 4,
  signature_data: 8,
  recovery_id: 72,
  _pad: 73,
} as const;

export class SignatureEntryView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 76);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): SignatureEntryView {
    return new SignatureEntryView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): SignatureEntryView {
    return new SignatureEntryView(bytes.buffer, bytes.byteOffset + offset);
  }

  get index(): number {
    return this.view.getUint32(0, true);
  }
  set index(v: number) {
    this.view.setUint32(0, v, true);
  }

  get error(): number {
    return this.view.getInt32(4, true);
  }
  set error(v: number) {
    this.view.setInt32(4, v, true);
  }

  get signature_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 8, 64);
  }

  get recovery_id(): number {
    return this.view.getInt8(72);
  }
  set recovery_id(v: number) {
    this.view.setInt8(72, v);
  }

  get _pad(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 73, 3);
  }

  toObject(): Record<string, unknown> {
    return {
      index: this.index,
      error: this.error,
      signature_data: Array.from(this.signature_data),
      recovery_id: this.recovery_id,
      _pad: Array.from(this._pad),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.index !== undefined) this.index = obj.index as number;
    if (obj.error !== undefined) this.error = obj.error as number;
    if (obj.signature_data !== undefined) {
      const arr = this.signature_data;
      const src = obj.signature_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.recovery_id !== undefined) this.recovery_id = obj.recovery_id as number;
    if (obj._pad !== undefined) {
      const arr = this._pad;
      const src = obj._pad as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): SignatureEntryView {
    return new SignatureEntryView(new ArrayBuffer(76));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 76);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 76);
  }
}

export class SignatureEntryArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): SignatureEntryArrayView {
    return new SignatureEntryArrayView(memory.buffer, ptr, count);
  }

  at(index: number): SignatureEntryView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new SignatureEntryView(this.buffer, this.baseOffset + index * 76);
  }

  *[Symbol.iterator](): Iterator<SignatureEntryView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const BATCHVERIFYREQUEST_SIZE = 44;
export const BATCHVERIFYREQUEST_ALIGN = 4;

export const BatchVerifyRequestOffsets = {
  public_key_data: 0,
  _pad0: 33,
  curve: 36,
  _pad1: 37,
  count: 40,
} as const;

export class BatchVerifyRequestView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 44);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): BatchVerifyRequestView {
    return new BatchVerifyRequestView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): BatchVerifyRequestView {
    return new BatchVerifyRequestView(bytes.buffer, bytes.byteOffset + offset);
  }

  get public_key_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 0, 33);
  }

  get _pad0(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 33, 3);
  }

  get curve(): number {
    return this.view.getUint8(36);
  }
  set curve(v: number) {
    this.view.setUint8(36, v);
  }

  get _pad1(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 37, 3);
  }

  get count(): number {
    return this.view.getUint32(40, true);
  }
  set count(v: number) {
    this.view.setUint32(40, v, true);
  }

  toObject(): Record<string, unknown> {
    return {
      public_key_data: Array.from(this.public_key_data),
      _pad0: Array.from(this._pad0),
      curve: this.curve,
      _pad1: Array.from(this._pad1),
      count: this.count,
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.public_key_data !== undefined) {
      const arr = this.public_key_data;
      const src = obj.public_key_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj._pad0 !== undefined) {
      const arr = this._pad0;
      const src = obj._pad0 as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.curve !== undefined) this.curve = obj.curve as number;
    if (obj._pad1 !== undefined) {
      const arr = this._pad1;
      const src = obj._pad1 as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.count !== undefined) this.count = obj.count as number;
  }

  static allocate(): BatchVerifyRequestView {
    return new BatchVerifyRequestView(new ArrayBuffer(44));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 44);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 44);
  }
}

export class BatchVerifyRequestArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): BatchVerifyRequestArrayView {
    return new BatchVerifyRequestArrayView(memory.buffer, ptr, count);
  }

  at(index: number): BatchVerifyRequestView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new BatchVerifyRequestView(this.buffer, this.baseOffset + index * 44);
  }

  *[Symbol.iterator](): Iterator<BatchVerifyRequestView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const VERIFYENTRY_SIZE = 100;
export const VERIFYENTRY_ALIGN = 4;

export const VerifyEntryOffsets = {
  index: 0,
  hash_data: 4,
  signature_data: 36,
} as const;

export class VerifyEntryView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 100);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): VerifyEntryView {
    return new VerifyEntryView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): VerifyEntryView {
    return new VerifyEntryView(bytes.buffer, bytes.byteOffset + offset);
  }

  get index(): number {
    return this.view.getUint32(0, true);
  }
  set index(v: number) {
    this.view.setUint32(0, v, true);
  }

  get hash_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 4, 32);
  }

  get signature_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 36, 64);
  }

  toObject(): Record<string, unknown> {
    return {
      index: this.index,
      hash_data: Array.from(this.hash_data),
      signature_data: Array.from(this.signature_data),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.index !== undefined) this.index = obj.index as number;
    if (obj.hash_data !== undefined) {
      const arr = this.hash_data;
      const src = obj.hash_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.signature_data !== undefined) {
      const arr = this.signature_data;
      const src = obj.signature_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): VerifyEntryView {
    return new VerifyEntryView(new ArrayBuffer(100));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 100);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 100);
  }
}

export class VerifyEntryArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): VerifyEntryArrayView {
    return new VerifyEntryArrayView(memory.buffer, ptr, count);
  }

  at(index: number): VerifyEntryView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new VerifyEntryView(this.buffer, this.baseOffset + index * 100);
  }

  *[Symbol.iterator](): Iterator<VerifyEntryView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const VERIFYRESULTENTRY_SIZE = 8;
export const VERIFYRESULTENTRY_ALIGN = 4;

export const VerifyResultEntryOffsets = {
  index: 0,
  error: 4,
} as const;

export class VerifyResultEntryView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 8);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): VerifyResultEntryView {
    return new VerifyResultEntryView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): VerifyResultEntryView {
    return new VerifyResultEntryView(bytes.buffer, bytes.byteOffset + offset);
  }

  get index(): number {
    return this.view.getUint32(0, true);
  }
  set index(v: number) {
    this.view.setUint32(0, v, true);
  }

  get error(): number {
    return this.view.getInt32(4, true);
  }
  set error(v: number) {
    this.view.setInt32(4, v, true);
  }

  toObject(): Record<string, unknown> {
    return {
      index: this.index,
      error: this.error,
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.index !== undefined) this.index = obj.index as number;
    if (obj.error !== undefined) this.error = obj.error as number;
  }

  static allocate(): VerifyResultEntryView {
    return new VerifyResultEntryView(new ArrayBuffer(8));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 8);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 8);
  }
}

export class VerifyResultEntryArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): VerifyResultEntryArrayView {
    return new VerifyResultEntryArrayView(memory.buffer, ptr, count);
  }

  at(index: number): VerifyResultEntryView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new VerifyResultEntryView(this.buffer, this.baseOffset + index * 8);
  }

  *[Symbol.iterator](): Iterator<VerifyResultEntryView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const STREAMDERIVECONFIG_SIZE = 132;
export const STREAMDERIVECONFIG_ALIGN = 4;

export const StreamDeriveConfigOffsets = {
  base_key_curve: 0,
  base_key_depth: 1,
  base_key__pad0: 2,
  base_key_parent_fingerprint: 4,
  base_key_child_index: 8,
  base_key_chain_code_data: 12,
  base_key_public_key_data: 44,
  base_key__pad1: 77,
  base_key_private_key_data: 80,
  base_key_has_private_key: 112,
  base_key__pad2: 113,
  start_index: 116,
  batch_size: 120,
  hardened: 124,
  _pad: 125,
} as const;

export class StreamDeriveConfigView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 132);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): StreamDeriveConfigView {
    return new StreamDeriveConfigView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): StreamDeriveConfigView {
    return new StreamDeriveConfigView(bytes.buffer, bytes.byteOffset + offset);
  }

  get base_key_curve(): number {
    return this.view.getUint8(0);
  }
  set base_key_curve(v: number) {
    this.view.setUint8(0, v);
  }

  get base_key_depth(): number {
    return this.view.getUint8(1);
  }
  set base_key_depth(v: number) {
    this.view.setUint8(1, v);
  }

  get base_key__pad0(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 2, 2);
  }

  get base_key_parent_fingerprint(): number {
    return this.view.getUint32(4, true);
  }
  set base_key_parent_fingerprint(v: number) {
    this.view.setUint32(4, v, true);
  }

  get base_key_child_index(): number {
    return this.view.getUint32(8, true);
  }
  set base_key_child_index(v: number) {
    this.view.setUint32(8, v, true);
  }

  get base_key_chain_code_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 12, 32);
  }

  get base_key_public_key_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 44, 33);
  }

  get base_key__pad1(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 77, 3);
  }

  get base_key_private_key_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 80, 32);
  }

  get base_key_has_private_key(): number {
    return this.view.getUint8(112);
  }
  set base_key_has_private_key(v: number) {
    this.view.setUint8(112, v);
  }

  get base_key__pad2(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 113, 3);
  }

  get start_index(): number {
    return this.view.getUint32(116, true);
  }
  set start_index(v: number) {
    this.view.setUint32(116, v, true);
  }

  get batch_size(): number {
    return this.view.getUint32(120, true);
  }
  set batch_size(v: number) {
    this.view.setUint32(120, v, true);
  }

  get hardened(): number {
    return this.view.getUint8(124);
  }
  set hardened(v: number) {
    this.view.setUint8(124, v);
  }

  get _pad(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 125, 7);
  }

  toObject(): Record<string, unknown> {
    return {
      base_key_curve: this.base_key_curve,
      base_key_depth: this.base_key_depth,
      base_key__pad0: Array.from(this.base_key__pad0),
      base_key_parent_fingerprint: this.base_key_parent_fingerprint,
      base_key_child_index: this.base_key_child_index,
      base_key_chain_code_data: Array.from(this.base_key_chain_code_data),
      base_key_public_key_data: Array.from(this.base_key_public_key_data),
      base_key__pad1: Array.from(this.base_key__pad1),
      base_key_private_key_data: Array.from(this.base_key_private_key_data),
      base_key_has_private_key: this.base_key_has_private_key,
      base_key__pad2: Array.from(this.base_key__pad2),
      start_index: this.start_index,
      batch_size: this.batch_size,
      hardened: this.hardened,
      _pad: Array.from(this._pad),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.base_key_curve !== undefined) this.base_key_curve = obj.base_key_curve as number;
    if (obj.base_key_depth !== undefined) this.base_key_depth = obj.base_key_depth as number;
    if (obj.base_key__pad0 !== undefined) {
      const arr = this.base_key__pad0;
      const src = obj.base_key__pad0 as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.base_key_parent_fingerprint !== undefined) this.base_key_parent_fingerprint = obj.base_key_parent_fingerprint as number;
    if (obj.base_key_child_index !== undefined) this.base_key_child_index = obj.base_key_child_index as number;
    if (obj.base_key_chain_code_data !== undefined) {
      const arr = this.base_key_chain_code_data;
      const src = obj.base_key_chain_code_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.base_key_public_key_data !== undefined) {
      const arr = this.base_key_public_key_data;
      const src = obj.base_key_public_key_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.base_key__pad1 !== undefined) {
      const arr = this.base_key__pad1;
      const src = obj.base_key__pad1 as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.base_key_private_key_data !== undefined) {
      const arr = this.base_key_private_key_data;
      const src = obj.base_key_private_key_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.base_key_has_private_key !== undefined) this.base_key_has_private_key = obj.base_key_has_private_key as number;
    if (obj.base_key__pad2 !== undefined) {
      const arr = this.base_key__pad2;
      const src = obj.base_key__pad2 as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.start_index !== undefined) this.start_index = obj.start_index as number;
    if (obj.batch_size !== undefined) this.batch_size = obj.batch_size as number;
    if (obj.hardened !== undefined) this.hardened = obj.hardened as number;
    if (obj._pad !== undefined) {
      const arr = this._pad;
      const src = obj._pad as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): StreamDeriveConfigView {
    return new StreamDeriveConfigView(new ArrayBuffer(132));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 132);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 132);
  }
}

export class StreamDeriveConfigArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): StreamDeriveConfigArrayView {
    return new StreamDeriveConfigArrayView(memory.buffer, ptr, count);
  }

  at(index: number): StreamDeriveConfigView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new StreamDeriveConfigView(this.buffer, this.baseOffset + index * 132);
  }

  *[Symbol.iterator](): Iterator<StreamDeriveConfigView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const STREAMSTATUS_SIZE = 16;
export const STREAMSTATUS_ALIGN = 4;

export const StreamStatusOffsets = {
  derived_count: 0,
  current_index: 4,
  error: 8,
  complete: 12,
  _pad: 13,
} as const;

export class StreamStatusView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 16);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): StreamStatusView {
    return new StreamStatusView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): StreamStatusView {
    return new StreamStatusView(bytes.buffer, bytes.byteOffset + offset);
  }

  get derived_count(): number {
    return this.view.getUint32(0, true);
  }
  set derived_count(v: number) {
    this.view.setUint32(0, v, true);
  }

  get current_index(): number {
    return this.view.getUint32(4, true);
  }
  set current_index(v: number) {
    this.view.setUint32(4, v, true);
  }

  get error(): number {
    return this.view.getInt32(8, true);
  }
  set error(v: number) {
    this.view.setInt32(8, v, true);
  }

  get complete(): number {
    return this.view.getUint8(12);
  }
  set complete(v: number) {
    this.view.setUint8(12, v);
  }

  get _pad(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 13, 3);
  }

  toObject(): Record<string, unknown> {
    return {
      derived_count: this.derived_count,
      current_index: this.current_index,
      error: this.error,
      complete: this.complete,
      _pad: Array.from(this._pad),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.derived_count !== undefined) this.derived_count = obj.derived_count as number;
    if (obj.current_index !== undefined) this.current_index = obj.current_index as number;
    if (obj.error !== undefined) this.error = obj.error as number;
    if (obj.complete !== undefined) this.complete = obj.complete as number;
    if (obj._pad !== undefined) {
      const arr = this._pad;
      const src = obj._pad as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): StreamStatusView {
    return new StreamStatusView(new ArrayBuffer(16));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 16);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 16);
  }
}

export class StreamStatusArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): StreamStatusArrayView {
    return new StreamStatusArrayView(memory.buffer, ptr, count);
  }

  at(index: number): StreamStatusView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new StreamStatusView(this.buffer, this.baseOffset + index * 16);
  }

  *[Symbol.iterator](): Iterator<StreamStatusView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const BATCHADDRESSREQUEST_SIZE = 8;
export const BATCHADDRESSREQUEST_ALIGN = 4;

export const BatchAddressRequestOffsets = {
  address_type: 0,
  network: 1,
  _pad: 2,
  count: 4,
} as const;

export class BatchAddressRequestView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 8);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): BatchAddressRequestView {
    return new BatchAddressRequestView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): BatchAddressRequestView {
    return new BatchAddressRequestView(bytes.buffer, bytes.byteOffset + offset);
  }

  get address_type(): number {
    return this.view.getUint8(0);
  }
  set address_type(v: number) {
    this.view.setUint8(0, v);
  }

  get network(): number {
    return this.view.getUint8(1);
  }
  set network(v: number) {
    this.view.setUint8(1, v);
  }

  get _pad(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 2, 2);
  }

  get count(): number {
    return this.view.getUint32(4, true);
  }
  set count(v: number) {
    this.view.setUint32(4, v, true);
  }

  toObject(): Record<string, unknown> {
    return {
      address_type: this.address_type,
      network: this.network,
      _pad: Array.from(this._pad),
      count: this.count,
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.address_type !== undefined) this.address_type = obj.address_type as number;
    if (obj.network !== undefined) this.network = obj.network as number;
    if (obj._pad !== undefined) {
      const arr = this._pad;
      const src = obj._pad as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj.count !== undefined) this.count = obj.count as number;
  }

  static allocate(): BatchAddressRequestView {
    return new BatchAddressRequestView(new ArrayBuffer(8));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 8);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 8);
  }
}

export class BatchAddressRequestArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): BatchAddressRequestArrayView {
    return new BatchAddressRequestArrayView(memory.buffer, ptr, count);
  }

  at(index: number): BatchAddressRequestView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new BatchAddressRequestView(this.buffer, this.baseOffset + index * 8);
  }

  *[Symbol.iterator](): Iterator<BatchAddressRequestView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const ADDRESSINPUTENTRY_SIZE = 40;
export const ADDRESSINPUTENTRY_ALIGN = 4;

export const AddressInputEntryOffsets = {
  index: 0,
  public_key_data: 4,
  _pad: 37,
} as const;

export class AddressInputEntryView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 40);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): AddressInputEntryView {
    return new AddressInputEntryView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): AddressInputEntryView {
    return new AddressInputEntryView(bytes.buffer, bytes.byteOffset + offset);
  }

  get index(): number {
    return this.view.getUint32(0, true);
  }
  set index(v: number) {
    this.view.setUint32(0, v, true);
  }

  get public_key_data(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 4, 33);
  }

  get _pad(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 37, 3);
  }

  toObject(): Record<string, unknown> {
    return {
      index: this.index,
      public_key_data: Array.from(this.public_key_data),
      _pad: Array.from(this._pad),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.index !== undefined) this.index = obj.index as number;
    if (obj.public_key_data !== undefined) {
      const arr = this.public_key_data;
      const src = obj.public_key_data as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj._pad !== undefined) {
      const arr = this._pad;
      const src = obj._pad as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): AddressInputEntryView {
    return new AddressInputEntryView(new ArrayBuffer(40));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 40);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 40);
  }
}

export class AddressInputEntryArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): AddressInputEntryArrayView {
    return new AddressInputEntryArrayView(memory.buffer, ptr, count);
  }

  at(index: number): AddressInputEntryView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new AddressInputEntryView(this.buffer, this.baseOffset + index * 40);
  }

  *[Symbol.iterator](): Iterator<AddressInputEntryView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

export const ADDRESSRESULTENTRY_SIZE = 72;
export const ADDRESSRESULTENTRY_ALIGN = 4;

export const AddressResultEntryOffsets = {
  index: 0,
  error: 4,
  address: 8,
  _pad: 70,
} as const;

export class AddressResultEntryView {
  private readonly view: DataView;

  constructor(buffer: ArrayBuffer, byteOffset = 0) {
    this.view = new DataView(buffer, byteOffset, 72);
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number): AddressResultEntryView {
    return new AddressResultEntryView(memory.buffer, ptr);
  }

  static fromBytes(bytes: Uint8Array, offset = 0): AddressResultEntryView {
    return new AddressResultEntryView(bytes.buffer, bytes.byteOffset + offset);
  }

  get index(): number {
    return this.view.getUint32(0, true);
  }
  set index(v: number) {
    this.view.setUint32(0, v, true);
  }

  get error(): number {
    return this.view.getInt32(4, true);
  }
  set error(v: number) {
    this.view.setInt32(4, v, true);
  }

  get address(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 8, 62);
  }

  get _pad(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset + 70, 2);
  }

  toObject(): Record<string, unknown> {
    return {
      index: this.index,
      error: this.error,
      address: Array.from(this.address),
      _pad: Array.from(this._pad),
    };
  }

  copyFrom(obj: Partial<Record<string, unknown>>): void {
    if (obj.index !== undefined) this.index = obj.index as number;
    if (obj.error !== undefined) this.error = obj.error as number;
    if (obj.address !== undefined) {
      const arr = this.address;
      const src = obj.address as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
    if (obj._pad !== undefined) {
      const arr = this._pad;
      const src = obj._pad as ArrayLike<number>;
      for (let i = 0; i < Math.min(arr.length, src.length); i++) arr[i] = src[i];
    }
  }

  static allocate(): AddressResultEntryView {
    return new AddressResultEntryView(new ArrayBuffer(72));
  }

  copyTo(dest: Uint8Array, offset = 0): void {
    const src = new Uint8Array(this.view.buffer, this.view.byteOffset, 72);
    dest.set(src, offset);
  }

  getBytes(): Uint8Array {
    return new Uint8Array(this.view.buffer, this.view.byteOffset, 72);
  }
}

export class AddressResultEntryArrayView {
  private readonly buffer: ArrayBuffer;
  private readonly baseOffset: number;
  readonly length: number;

  constructor(buffer: ArrayBuffer, byteOffset: number, count: number) {
    this.buffer = buffer;
    this.baseOffset = byteOffset;
    this.length = count;
  }

  static fromMemory(memory: WebAssembly.Memory, ptr: number, count: number): AddressResultEntryArrayView {
    return new AddressResultEntryArrayView(memory.buffer, ptr, count);
  }

  at(index: number): AddressResultEntryView {
    if (index < 0 || index >= this.length) {
      throw new RangeError(`Index ${index} out of bounds [0, ${this.length})`);
    }
    return new AddressResultEntryView(this.buffer, this.baseOffset + index * 72);
  }

  *[Symbol.iterator](): Iterator<AddressResultEntryView> {
    for (let i = 0; i < this.length; i++) {
      yield this.at(i);
    }
  }
}

