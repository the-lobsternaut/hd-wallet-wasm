name: hd_wallet_wasm_test
description: Dart tests for hd-wallet-wasm WASI module
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  wasm_run: ^0.1.0+2

dev_dependencies:
  test: ^1.24.0
  convert: ^3.1.1
  crypto: ^3.0.3
